{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["cd /app/apps/web && npm install --legacy-peer-deps"]
    },
    "build": {
      "commands": ["cd /app/apps/web && npm run build"]
    }
  },
  "deploy": {
    "startCommand": "cd /app/apps/web && node build/server/index.js"
  }
}
