{
  "name": "list-organizer-chat",
  "private": true,
  "scripts": {
    "start": "npm start --prefix apps/web",
    "build": "cd apps/web && npm run build",
    "build:web": "cd apps/web && npm ci && npm run build"
  }
}
