{
  "name": "list-organizer-chat",
  "private": true,
  "scripts": {
    "start": "npm start --prefix apps/web",
    "build": "cd apps/web && npm run build && npm run check:server-build-cycle",
    "build:web": "cd apps/web && npm ci && npm run build && npm run check:server-build-cycle"
  }
}
